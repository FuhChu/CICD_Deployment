// package.json
{
  "name": "node-app",
  "version": "1.0.0",
  "description": "A simple Node.js app for DevOps pipeline",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "dependencies": {
    "express": "^4.18.2" // Ensure express is listed as a dependency
  }
}
